# Pomodoro Timer 段階的実装計画

このドキュメントは、UIモック・アーキテクチャ・機能一覧を踏まえた段階的な実装計画を定義する。

参照:
- architecture.md
- features.md

## 実装粒度の基本ルール

1. 1ステップ = 1つのユーザー価値 + 最小テスト + デモ可能状態
2. 1PRの目安は変更ファイル3〜7個、レビュー時間30分以内
3. UI変更PRとロジック変更PRを可能な限り分離する
4. 各ステップに手動確認項目と自動テスト項目を含める

## ステップ0: 土台構築

### 目的

Flaskアプリとして開発を開始できる最小土台を用意する。

### 実装内容

1. Flaskエントリポイント整備
2. テンプレート配信（`GET /`）
3. 静的ファイル配信（CSS/JavaScript）
4. 起動手順の明文化

### 完了条件

1. トップページが表示される
2. CSS/JSが読み込まれる
3. 開発起動手順がREADMEに記載されている

## ステップ1: モック準拠の静的UI

### 目的

添付モックに近い見た目を先に確定し、後続実装の手戻りを防ぐ。

### 実装内容

1. 全体レイアウト
2. タイマー表示エリア
3. 円形プログレス（静的）
4. ボタン群（開始/リセット）
5. 今日の進捗カード

### 完了条件

1. モックに近いUIが表示される
2. モバイル幅でも崩れない
3. 要素ID/クラスが確定している

## ステップ2: ドメインロジック先行実装（純粋関数）

### 目的

テストしやすい中核ロジックをUIから独立して実装する。

### 実装内容

1. 状態定義（idle/running/paused/onBreak/finished）
2. 状態遷移reducer
3. 残り時間計算
4. 進捗集計

### 完了条件

1. 開始/一時停止/再開/リセット遷移の単体テストが通る
2. 時間計算の境界値テストが通る
3. DOM依存コードが存在しない

## ステップ3: Application層によるUI接続

### 目的

ドメインロジックと画面を接続し、MVPの主機能を成立させる。

### 実装内容

1. ボタンイベント配線
2. 1秒ごとの表示更新
3. 円形プログレスの動的更新
4. セッション完了時の状態反映

### 完了条件

1. 25:00からのカウントダウンが動作する
2. 一時停止と再開が正しく動作する
3. 完了時にUI状態が正しく切り替わる

## ステップ4: 永続化と日次進捗

### 目的

リロード耐性と進捗可視化を完成させる。

### 実装内容

1. 設定値保存（localStorage）
2. 当日進捗保存（localStorage）
3. 日付変更時の日次進捗リセット

### 完了条件

1. リロード後も設定と進捗が維持される
2. 日付変更時に当日データが新規化される
3. 保存データ破損時に安全に初期化される

## ステップ5: 品質強化（時間ずれ・例外・テスト拡張）

### 目的

実運用で壊れにくい品質に引き上げる。

### 実装内容

1. endAt基準の時間計算
2. タブ非アクティブ復帰時の補正
3. 入力バリデーション
4. localStorage異常時のエラーハンドリング
5. テストケース拡張

### 完了条件

1. タブ復帰後も残り時間が妥当である
2. 不正設定値入力でもアプリが停止しない
3. 主要分岐の単体テストが通る

## ステップ6: 拡張機能

### 目的

MVPを壊さず価値を追加する。

### 実装優先順

1. セッション完了音/ブラウザ通知
2. 長休憩ルール強化
3. 履歴表示
4. Flask API + SQLite保存

### 完了条件

1. 各拡張が既存MVPを破壊しない
2. 追加機能ごとの回帰確認項目がある

## チケット分割ルール

1. 1チケットは半日〜1日で完了できる粒度に分割する
2. 受け入れ条件は3項目以内にする
3. ステップ終了ごとに固定のデモシナリオで回帰確認する

## デモシナリオ（固定）

1. 起動して初期表示を確認
2. 開始 -> 一時停止 -> 再開
3. 完了時の表示切替と進捗反映
4. リロード後の状態復元
5. 日付変更時の進捗初期化

## 補足

段階ごとに「動くもの」を作り、次段階で品質と拡張性を積み上げる方針とする。これにより、実装の途中でも価値検証と軌道修正が可能になる。
